# Installation Guide

To set up the necessary environment for this project, install the required packages by executing these commands in your terminal:

```bash
python -m pip install django
pip install djangorestframework
